@page
@model Randevoo.AdminPanel.Pages.Participants.IndexModel
@{
    ViewData["Title"] = Model.IsEventScoped && !string.IsNullOrWhiteSpace(Model.EventTitle)
        ? $"فهرست شرکت‌کنندگان {Model.EventTitle}"
        : "فهرست شرکت‌کنندگان";
}

<section class="page-header admin-participants-header">
    <div>
        <span class="page-kicker">مدیریت کاربران شرکت‌کننده</span>
        <h1>@ViewData["Title"]</h1>
        @if (Model.IsAdminDirectory)
        {
            <p class="text-muted mb-0">فهرست واحد ادمین برای مدیریت پروفایل، وضعیت حساب، رویدادها، مالی و سوابق عملیاتی شرکت‌کنندگان.</p>
        }
        else if (Model.IsEventScoped)
        {
            <p class="text-muted mb-0">نمای حضور شرکت‌کنندگان همین رویداد، با عملیات متناسب با سطح دسترسی.</p>
        }
        else
        {
            <p class="text-muted mb-0">نمای سراسری شرکت‌کنندگان رویدادها با فیلتر، مرتب‌سازی و صفحه‌بندی سمت سرور.</p>
        }
    </div>
    <div class="d-flex flex-wrap gap-2">
        @if (Model.IsAdmin)
        {
            <a asp-page="/Settings/OperationPermissions" class="btn btn-outline-primary">
                <i class="bi bi-shield-lock"></i>سطوح دسترسی عملیات
            </a>
        }
        @if (Model.IsEventScoped && Model.EventId.HasValue)
        {
            <a asp-page="/Buyers/Index" asp-route-EventId="@Model.EventId" class="btn btn-outline-secondary">
                <i class="bi bi-bag-check"></i>خریداران این رویداد
            </a>
        }
        <a asp-page="/Events/Index" asp-route-Scope="Active" class="btn btn-outline-secondary">
            <i class="bi bi-calendar3"></i>رویدادها
        </a>
    </div>
</section>

@if (!string.IsNullOrWhiteSpace(Model.StatusMessage))
{
    <div class="alert alert-success border-0 mb-4">@Model.StatusMessage</div>
}

<div asp-validation-summary="ModelOnly" class="alert alert-danger border-0 mb-4"></div>

<section class="participant-kpi-grid admin-participants-kpis mb-4" aria-label="خلاصه شرکت‌کنندگان">
    <article class="participant-kpi-card">
        <span class="participant-kpi-icon"><i class="bi bi-people"></i></span>
        <div>
            <span>@(Model.IsAdminDirectory ? "کاربران شرکت‌کننده" : "کل شرکت‌کنندگان")</span>
            <strong>@DisplayFormatter.Count(Model.Result.Summary.TotalParticipants)</strong>
        </div>
    </article>
    @if (Model.Result.Summary.AvailableCapacity.HasValue)
    {
        <article class="participant-kpi-card">
            <span class="participant-kpi-icon"><i class="bi bi-ticket-perforated"></i></span>
            <div>
                <span>ظرفیت خالی</span>
                <strong>@DisplayFormatter.Count(Model.Result.Summary.AvailableCapacity.Value)</strong>
            </div>
        </article>
    }
    <article class="participant-kpi-card">
        <span class="participant-kpi-icon"><i class="bi bi-person-check"></i></span>
        <div>
            <span>پروفایل کامل</span>
            <strong>@DisplayFormatter.Count(Model.Result.Summary.CompletedProfiles)</strong>
        </div>
    </article>
    <article class="participant-kpi-card">
        <span class="participant-kpi-icon"><i class="bi bi-bag-check"></i></span>
        <div>
            <span>@(Model.IsAdminDirectory ? "دارای بلیت" : "کل خریداران بلیت")</span>
            <strong>@DisplayFormatter.Count(Model.Result.Summary.TotalBuyers)</strong>
        </div>
    </article>
    <article class="participant-kpi-card is-warning">
        <span class="participant-kpi-icon"><i class="bi bi-arrow-counterclockwise"></i></span>
        <div>
            <span>لغو یا برگشت</span>
            <strong>@DisplayFormatter.Count(Model.Result.Summary.CancelledTickets)</strong>
        </div>
    </article>
</section>

<form method="get" class="glass-panel p-4 mb-4 filter-panel admin-participants-filter">
    <div class="d-flex justify-content-between align-items-center flex-wrap gap-3 mb-3">
        <div>
            <h2 class="section-title mb-1">فیلتر شرکت‌کنندگان</h2>
            <p class="text-muted small mb-0">جستجو، فیلتر، مرتب‌سازی و تعداد صفحه روی دیتابیس اعمال می‌شود.</p>
        </div>
        <button class="btn btn-outline-secondary btn-sm filter-toggle-button" type="button" data-bs-toggle="collapse" data-bs-target="#participantsFilterPanel" aria-expanded="@(Model.HasActiveFilters.ToString().ToLowerInvariant())" aria-controls="participantsFilterPanel">
            <i class="bi bi-funnel"></i>فیلترها
        </button>
    </div>
    <div id="participantsFilterPanel" class="collapse @(Model.HasActiveFilters ? "show" : string.Empty)">
        @if (Model.EventId.HasValue)
        {
            <input type="hidden" asp-for="EventId" />
        }
        @if (Model.BuyerUserId.HasValue)
        {
            <input type="hidden" asp-for="BuyerUserId" />
        }
        @if (Model.TicketOrderId.HasValue)
        {
            <input type="hidden" asp-for="TicketOrderId" />
        }
        <input type="hidden" name="PageNumber" value="1" />
        <input type="hidden" asp-for="View" />

        @if (Model.IsAdminDirectory)
        {
            <div class="row g-3 align-items-end">
                <div class="col-xl-3 col-lg-4">
                    <label asp-for="Search" class="form-label">جستجو</label>
                    <input asp-for="Search" class="form-control" placeholder="نام، موبایل یا ایمیل" />
                </div>
                <div class="col-xl-2 col-lg-3 col-md-4">
                    <label asp-for="CityId" class="form-label">شهر</label>
                    <select asp-for="CityId" asp-items="Model.CityOptions" class="form-select">
                        <option value="">همه شهرها</option>
                    </select>
                </div>
                <div class="col-xl-2 col-lg-3 col-md-4">
                    <label asp-for="FilterEventId" class="form-label">رویداد</label>
                    <select asp-for="FilterEventId" asp-items="Model.EventOptions" class="form-select">
                        <option value="">همه رویدادها</option>
                    </select>
                </div>
                <div class="col-xl-1 col-lg-2 col-md-4">
                    <label asp-for="Gender" class="form-label">جنسیت</label>
                    <select asp-for="Gender" class="form-select">
                        <option value="">همه</option>
                        <option value="male">آقا</option>
                        <option value="female">خانم</option>
                    </select>
                </div>
                <div class="col-xl-2 col-lg-3 col-md-4">
                    <label asp-for="ZodiacSignId" class="form-label">زودیاک</label>
                    <select asp-for="ZodiacSignId" asp-items="Model.ZodiacSignOptions" class="form-select">
                        <option value="">همه</option>
                    </select>
                </div>
                <div class="col-xl-2 col-lg-3 col-md-4">
                    <label asp-for="ProfileStatus" class="form-label">وضعیت پروفایل</label>
                    <select asp-for="ProfileStatus" class="form-select">
                        <option value="">همه</option>
                        <option value="completed">پروفایل کامل</option>
                        <option value="pending">در انتظار تکمیل</option>
                    </select>
                </div>
                <div class="col-xl-2 col-lg-3 col-md-4">
                    <label asp-for="TicketStatus" class="form-label">وضعیت بلیت</label>
                    <select asp-for="TicketStatus" class="form-select">
                        <option value="">همه</option>
                        <option value="active">دارای بلیت فعال</option>
                        <option value="refunded">دارای برگشت وجه</option>
                        <option value="removed">حذف اضطراری</option>
                        <option value="none">بدون بلیت</option>
                    </select>
                </div>
                <div class="col-xl-1 col-lg-2 col-md-4">
                    <label asp-for="IsActive" class="form-label">حساب</label>
                    <select asp-for="IsActive" class="form-select">
                        <option value="">همه</option>
                        <option value="true">فعال</option>
                        <option value="false">غیرفعال</option>
                    </select>
                </div>
                <div class="col-xl-2 col-lg-3 col-md-4">
                    <label asp-for="Sort" class="form-label">مرتب‌سازی</label>
                    <select asp-for="Sort" class="form-select">
                        <option value="last-activity">آخرین فعالیت</option>
                        <option value="registration-desc">ثبت‌نام جدیدتر</option>
                        <option value="registration-asc">ثبت‌نام قدیمی‌تر</option>
                        <option value="name">نام شرکت‌کننده</option>
                        <option value="tickets-desc">بلیت بیشتر</option>
                        <option value="events-desc">رویداد بیشتر</option>
                        <option value="profile-status">وضعیت پروفایل</option>
                        <option value="activity-asc">فعالیت قدیمی‌تر</option>
                    </select>
                </div>
                <div class="col-lg-2 col-md-4">
                    <label asp-for="FromDate" class="form-label">ثبت‌نام از</label>
                    <input asp-for="FromDate" class="form-control" type="date" />
                </div>
                <div class="col-lg-2 col-md-4">
                    <label asp-for="ToDate" class="form-label">ثبت‌نام تا</label>
                    <input asp-for="ToDate" class="form-control" type="date" />
                </div>
                <div class="col-lg-2 col-md-4">
                    <label asp-for="PageSize" class="form-label">تعداد در صفحه</label>
                    <select asp-for="PageSize" class="form-select">
                        <option value="25">۲۵</option>
                        <option value="50">۵۰</option>
                        <option value="100">۱۰۰</option>
                    </select>
                </div>
                <div class="col-12 d-flex justify-content-end gap-2">
                    <a asp-page="/Participants/Index" class="btn btn-outline-secondary">پاک کردن</a>
                    <button type="submit" class="btn btn-primary"><i class="bi bi-search"></i>اعمال فیلتر</button>
                </div>
            </div>
        }
        else
        {
            <div class="row g-3 align-items-end">
                <div class="col-xl-3 col-lg-4">
                    <label asp-for="Search" class="form-label">جستجو</label>
                    <input asp-for="Search" class="form-control" placeholder="نام، کد رویداد، موبایل، شماره بلیت" />
                </div>
                @if (!Model.IsEventScoped)
                {
                    <div class="col-xl-2 col-lg-3">
                        <label asp-for="FilterEventId" class="form-label">رویداد</label>
                        <select asp-for="FilterEventId" asp-items="Model.EventOptions" class="form-select">
                            <option value="">همه رویدادها</option>
                        </select>
                    </div>
                }
                <div class="col-xl-2 col-lg-3 col-md-4">
                    <label asp-for="ProfileStatus" class="form-label">وضعیت پروفایل</label>
                    <select asp-for="ProfileStatus" class="form-select">
                        <option value="">همه</option>
                        <option value="completed">پروفایل کامل</option>
                        <option value="pending">در انتظار تکمیل</option>
                    </select>
                </div>
                <div class="col-xl-2 col-lg-3 col-md-4">
                    <label asp-for="TicketStatus" class="form-label">وضعیت بلیت</label>
                    <select asp-for="TicketStatus" class="form-select">
                        <option value="">همه</option>
                        <option value="active">فعال</option>
                        <option value="refunded">بازگشت وجه</option>
                        <option value="removed">حذف اضطراری</option>
                    </select>
                </div>
                <div class="col-xl-1 col-lg-2 col-md-4">
                    <label asp-for="Gender" class="form-label">جنسیت</label>
                    <select asp-for="Gender" class="form-select">
                        <option value="">همه</option>
                        <option value="male">آقا</option>
                        <option value="female">خانم</option>
                    </select>
                </div>
                <div class="col-xl-2 col-lg-3 col-md-4">
                    <label asp-for="Sort" class="form-label">مرتب‌سازی</label>
                    <select asp-for="Sort" class="form-select">
                        <option value="purchase-desc">خرید جدیدتر</option>
                        <option value="purchase-asc">خرید قدیمی‌تر</option>
                        <option value="name">نام شرکت‌کننده</option>
                        <option value="event">نام رویداد</option>
                        <option value="profile-status">وضعیت پروفایل</option>
                        <option value="payment-status">وضعیت بلیت</option>
                        <option value="price-desc">مبلغ بیشتر</option>
                        <option value="price-asc">مبلغ کمتر</option>
                    </select>
                </div>
                <div class="col-lg-2 col-md-4">
                    <label asp-for="FromDate" class="form-label">از تاریخ خرید</label>
                    <input asp-for="FromDate" class="form-control" type="date" />
                </div>
                <div class="col-lg-2 col-md-4">
                    <label asp-for="ToDate" class="form-label">تا تاریخ خرید</label>
                    <input asp-for="ToDate" class="form-control" type="date" />
                </div>
                <div class="col-lg-2 col-md-4">
                    <label asp-for="PageSize" class="form-label">تعداد در صفحه</label>
                    <select asp-for="PageSize" class="form-select">
                        <option value="25">۲۵</option>
                        <option value="50">۵۰</option>
                        <option value="100">۱۰۰</option>
                    </select>
                </div>
                <div class="col-12 d-flex justify-content-end gap-2">
                    <a asp-page="/Participants/Index" asp-route-EventId="@Model.EventId" class="btn btn-outline-secondary">پاک کردن</a>
                    <button type="submit" class="btn btn-primary"><i class="bi bi-search"></i>اعمال فیلتر</button>
                </div>
            </div>
        }
    </div>
</form>

@if (Model.IsAdminDirectory)
{
    <section class="glass-panel admin-participants-panel">
        <div class="admin-events-list-head">
            <div>
                <h2 class="section-title mb-1">فهرست شرکت‌کنندگان</h2>
                <p class="text-muted small mb-0">@DisplayFormatter.Count(Model.Result.TotalCount) کاربر مطابق فیلتر فعلی</p>
            </div>
            <span class="admin-events-page-meter">صفحه @DisplayFormatter.Count(Model.PageNumber) از @DisplayFormatter.Count(Model.TotalPages)</span>
        </div>

        <div class="admin-participants-table-wrap">
            <table class="admin-participants-table">
                <colgroup>
                    <col class="admin-participants-col-identity" />
                    <col class="admin-participants-col-profile" />
                    <col class="admin-participants-col-stats" />
                    <col class="admin-participants-col-event" />
                    <col class="admin-participants-col-activity" />
                    <col class="admin-participants-col-status" />
                    <col class="admin-participants-col-actions" />
                </colgroup>
                <thead>
                    <tr>
                        <th>شرکت‌کننده</th>
                        <th>پروفایل</th>
                        <th>بلیت و رویداد</th>
                        <th>آخرین رویداد</th>
                        <th>فعالیت</th>
                        <th>وضعیت</th>
                        <th>عملیات</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.Result.Items)
                    {
                        var actionRowId = $"participant-actions-{item.ParticipantUserId}";
                        <tr class="admin-participant-row">
                            <td data-label="شرکت‌کننده">
                                <div class="admin-participant-identity">
                                    <img src="@(item.ProfileImageUrl ?? "/images/logo.png")" alt="@item.ParticipantName" class="admin-participant-avatar" loading="lazy" />
                                    <div>
                                        <strong>@item.ParticipantName</strong>
                                        @if (Model.CanSeeContactInfo && !string.IsNullOrWhiteSpace(item.ParticipantMobile))
                                        {
                                            <span dir="ltr">@item.ParticipantMobile</span>
                                        }
                                        <small>ثبت‌نام: @PersianDateFormatter.FormatDate(item.CreatedAtUtc, Model.IsRtl)</small>
                                    </div>
                                </div>
                            </td>
                            <td data-label="پروفایل">
                                <div class="admin-participant-profile-stack">
                                    <span class="status-badge @Model.GetProfileStatusClass(item)">@item.ProfileStatusTitle</span>
                                    <small>@item.GenderTitle، @(item.Age > 0 ? $"{DisplayFormatter.Count(item.Age)} ساله" : "سن ثبت نشده")</small>
                                    <small>@item.CityTitle · @item.EducationLevelTitle</small>
                                </div>
                            </td>
                            <td data-label="بلیت و رویداد">
                                <div class="admin-participant-stats">
                                    <strong>@DisplayFormatter.Count(item.TicketCount) بلیت</strong>
                                    <span>@DisplayFormatter.Count(item.EventCount) رویداد، @DisplayFormatter.Count(item.ActiveTicketCount) فعال</span>
                                    @if (item.CancelledTicketCount > 0)
                                    {
                                        <small>@DisplayFormatter.Count(item.CancelledTicketCount) لغو یا برگشت</small>
                                    }
                                </div>
                            </td>
                            <td data-label="آخرین رویداد">
                                <div class="admin-participant-last-event">
                                    @if (item.EventId.HasValue)
                                    {
                                        <a asp-page="/Events/Details" asp-route-id="@item.EventId">@item.EventTitle</a>
                                        <span>@item.EventPlannerName</span>
                                    }
                                    else
                                    {
                                        <strong>بدون رویداد</strong>
                                        <span>هنوز بلیتی ثبت نشده</span>
                                    }
                                </div>
                            </td>
                            <td data-label="فعالیت">
                                <div class="admin-participant-activity">
                                    <strong>@(item.LastActivityAtUtc.HasValue ? PersianDateFormatter.FormatDateTime(item.LastActivityAtUtc.Value, Model.IsRtl) : "بدون لاگ")</strong>
                                    <span>@(item.LastTicketAtUtc.HasValue ? $"آخرین بلیت: {PersianDateFormatter.FormatDate(item.LastTicketAtUtc.Value, Model.IsRtl)}" : "بدون خرید بلیت")</span>
                                    <span>@DisplayFormatter.Count(item.SupportTicketCount) تیکت پشتیبانی</span>
                                </div>
                            </td>
                            <td data-label="وضعیت">
                                <span class="status-badge @Model.GetTicketStatusClass(item)">@item.TicketStatus</span>
                            </td>
                            <td class="admin-participant-actions-cell" data-label="عملیات">
                                <button class="btn btn-sm btn-outline-primary admin-event-actions-toggle" type="button" data-bs-toggle="collapse" data-bs-target="#@actionRowId" aria-expanded="false" aria-controls="@actionRowId">
                                    عملیات
                                    <i class="bi bi-chevron-down admin-event-actions-chevron"></i>
                                </button>
                            </td>
                        </tr>
                        <tr class="admin-participant-actions-row">
                            <td colspan="7">
                                <div class="collapse" id="@actionRowId">
                                    <div class="admin-participant-actions-panel">
                                        <div class="admin-event-action-summary">
                                            <span>شناسه کاربر</span>
                                            <strong>@DisplayFormatter.Count((int)item.ParticipantUserId)</strong>
                                        </div>
                                        <div class="admin-event-action-group">
                                            <h3>پروفایل</h3>
                                        @if (Model.AllowedActions.Contains("viewDetails") && item.HasProfile)
                                        {
                                            <a asp-page="/UserProfiles/Details" asp-route-userId="@item.ParticipantUserId" class="admin-event-action-link">
                                                <i class="bi bi-person-bounding-box"></i>مشاهده جزئیات
                                            </a>
                                        }
                                        @if (Model.AllowedActions.Contains("editProfile") && item.HasProfile)
                                        {
                                            <a asp-page="/UserProfiles/AdminEdit" asp-route-userId="@item.ParticipantUserId" class="admin-event-action-link">
                                                <i class="bi bi-person-gear"></i>ویرایش پروفایل
                                            </a>
                                        }
                                        @if (!item.HasProfile)
                                        {
                                            <span class="admin-event-action-link is-disabled"><i class="bi bi-person-x"></i>پروفایل ثبت نشده</span>
                                        }
                                        </div>
                                        <div class="admin-event-action-group">
                                            <h3>مالی و بلیت</h3>
                                            @if (Model.AllowedActions.Contains("viewFinance"))
                                            {
                                                <a asp-page="/Finance/User" asp-route-userId="@item.ParticipantUserId" asp-route-view="balance" class="admin-event-action-link">
                                                    <i class="bi bi-wallet2"></i>کیف پول و مالی
                                                </a>
                                            }
                                            @if (Model.AllowedActions.Contains("viewOrder") && item.EventId.HasValue)
                                            {
                                                <a asp-page="/Finance/TicketTransactions" asp-route-EventId="@item.EventId" asp-route-Search="@item.ParticipantMobile" class="admin-event-action-link">
                                                    <i class="bi bi-receipt-cutoff"></i>تراکنش‌های بلیت
                                                </a>
                                            }
                                            else
                                            {
                                                <span class="admin-event-action-link is-disabled"><i class="bi bi-receipt"></i>بدون تراکنش بلیت</span>
                                            }
                                        </div>
                                        <div class="admin-event-action-group">
                                            <h3>سوابق</h3>
                                            <a asp-page="/Logs/Index" asp-route-Search="@item.ParticipantMobile" class="admin-event-action-link">
                                                <i class="bi bi-journal-text"></i>لاگ فعالیت
                                            </a>
                                            <a asp-page="/Support/Tickets" asp-route-SubmitterRole="EndUser" class="admin-event-action-link">
                                                <i class="bi bi-headset"></i>تیکت‌های پشتیبانی
                                            </a>
                                            @if (item.EventId.HasValue)
                                            {
                                                <a asp-page="/Participants/Index" asp-route-EventId="@item.EventId" asp-route-Search="@item.ParticipantMobile" class="admin-event-action-link">
                                                    <i class="bi bi-calendar-check"></i>حضور در آخرین رویداد
                                                </a>
                                            }
                                        </div>
                                    </div>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        @if (Model.Result.Items.Count == 0)
        {
            <div class="admin-events-empty">
                <i class="bi bi-person-x"></i>
                <strong>شرکت‌کننده‌ای پیدا نشد</strong>
                <span>فیلترها را تغییر بدهید یا جستجو را پاک کنید.</span>
            </div>
        }
    </section>
}
else
{
    <section class="glass-panel p-4">
        <div class="d-flex flex-wrap align-items-center justify-content-between gap-3 mb-4">
            <div>
                <h2 class="section-title mb-1">شرکت‌کنندگان</h2>
                <p class="text-muted small mb-0">@DisplayFormatter.Count(Model.Result.TotalCount) رکورد مطابق فیلتر فعلی</p>
            </div>
            <div class="btn-group view-toggle" role="group" aria-label="تغییر نمایش">
                <a asp-page="/Participants/Index"
                   asp-route-EventId="@Model.EventId"
                   asp-route-Search="@Model.Search"
                   asp-route-ProfileStatus="@Model.ProfileStatus"
                   asp-route-TicketStatus="@Model.TicketStatus"
                   asp-route-Gender="@Model.Gender"
                   asp-route-FilterEventId="@Model.FilterEventId"
                   asp-route-BuyerUserId="@Model.BuyerUserId"
                   asp-route-TicketOrderId="@Model.TicketOrderId"
                   asp-route-FromDate="@Model.FromDate?.ToString("yyyy-MM-dd")"
                   asp-route-ToDate="@Model.ToDate?.ToString("yyyy-MM-dd")"
                   asp-route-Sort="@Model.Sort"
                   asp-route-PageSize="@Model.PageSize"
                   asp-route-View="table"
                   class="btn btn-sm @(Model.IsCardView ? "btn-outline-primary" : "btn-primary")">
                    <i class="bi bi-table"></i>جدول
                </a>
                <a asp-page="/Participants/Index"
                   asp-route-EventId="@Model.EventId"
                   asp-route-Search="@Model.Search"
                   asp-route-ProfileStatus="@Model.ProfileStatus"
                   asp-route-TicketStatus="@Model.TicketStatus"
                   asp-route-Gender="@Model.Gender"
                   asp-route-FilterEventId="@Model.FilterEventId"
                   asp-route-BuyerUserId="@Model.BuyerUserId"
                   asp-route-TicketOrderId="@Model.TicketOrderId"
                   asp-route-FromDate="@Model.FromDate?.ToString("yyyy-MM-dd")"
                   asp-route-ToDate="@Model.ToDate?.ToString("yyyy-MM-dd")"
                   asp-route-Sort="@Model.Sort"
                   asp-route-PageSize="@Model.PageSize"
                   asp-route-View="cards"
                   class="btn btn-sm @(Model.IsCardView ? "btn-primary" : "btn-outline-primary")">
                    <i class="bi bi-person-square"></i>کارت
                </a>
            </div>
        </div>

        @if (!Model.IsCardView)
        {
            <div class="table-responsive">
                <table class="table align-middle">
                    <thead>
                        <tr>
                            <th>شرکت‌کننده</th>
                            <th>خریدار/سفارش</th>
                            @if (Model.CanSeeContactInfo)
                            {
                                <th>تماس</th>
                            }
                            @if (!Model.IsEventScoped)
                            {
                                <th>رویداد</th>
                            }
                            <th>پروفایل</th>
                            <th>جنسیت</th>
                            <th>سن</th>
                            <th>شهر</th>
                            <th>بلیت</th>
                            <th>تاریخ خرید</th>
                            <th>وضعیت</th>
                            <th>عملیات</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model.Result.Items)
                        {
                            <tr>
                                <td>
                                    <div class="buyer-profile-cell">
                                        <img src="@(item.ProfileImageUrl ?? "/images/logo.png")" alt="@item.ParticipantName" class="buyer-avatar" loading="lazy" />
                                        <div>
                                            <div class="fw-semibold">@item.ParticipantName</div>
                                            <div class="text-muted small">شناسه بلیت: @DisplayFormatter.Count((int)item.TicketId)</div>
                                        </div>
                                    </div>
                                </td>
                                <td>
                                    <div class="fw-semibold">@item.BuyerName</div>
                                    @if (item.TicketOrderId is long orderId)
                                    {
                                        <a asp-page="/Buyers/Index" asp-route-TicketOrderId="@orderId" class="text-muted small">
                                            سفارش #@DisplayFormatter.Count((int)orderId)
                                        </a>
                                    }
                                </td>
                                @if (Model.CanSeeContactInfo)
                                {
                                    <td dir="ltr">@item.ParticipantMobile</td>
                                }
                                @if (!Model.IsEventScoped)
                                {
                                    <td>
                                        <div class="fw-semibold">@item.EventTitle</div>
                                        <div class="text-muted small">@item.EventPlannerName</div>
                                    </td>
                                }
                                <td><span class="status-badge @Model.GetProfileStatusClass(item)">@item.ProfileStatusTitle</span></td>
                                <td>@item.GenderTitle</td>
                                <td>@(item.Age > 0 ? DisplayFormatter.Count(item.Age) : "ثبت نشده")</td>
                                <td>@item.CityTitle</td>
                                <td>@DisplayFormatter.TicketMoney(item.TicketPrice, item.TicketCurrencyCode, Model.IsRtl)</td>
                                <td>@PersianDateFormatter.FormatDateTime(item.PurchasedAtUtc, Model.IsRtl)</td>
                                <td>
                                    <span class="status-badge @Model.GetTicketStatusClass(item)">@item.TicketStatus</span>
                                    @if (!string.IsNullOrWhiteSpace(item.RemovalReason))
                                    {
                                        <div class="text-muted small mt-2">@item.RemovalReason</div>
                                    }
                                </td>
                                <td class="table-action-cell">
                                    <div class="dropdown">
                                        <button class="btn btn-sm btn-outline-secondary dropdown-toggle table-action-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                                            عملیات
                                        </button>
                                        <div class="dropdown-menu dropdown-menu-end action-menu shadow-sm border-0">
                                            @if (Model.AllowedActions.Contains("viewDetails") && item.HasProfile)
                                            {
                                                <a asp-page="/UserProfiles/Details" asp-route-userId="@item.ParticipantUserId" class="dropdown-item">
                                                    <i class="bi bi-person-bounding-box"></i>مشاهده جزئیات
                                                </a>
                                            }
                                            @if (Model.IsAdmin && Model.AllowedActions.Contains("editProfile") && item.HasProfile)
                                            {
                                                <a asp-page="/UserProfiles/AdminEdit" asp-route-userId="@item.ParticipantUserId" class="dropdown-item">
                                                    <i class="bi bi-person-gear"></i>ویرایش پروفایل
                                                </a>
                                            }
                                            @if (Model.IsAdmin && Model.AllowedActions.Contains("viewFinance"))
                                            {
                                                <a asp-page="/Finance/User" asp-route-userId="@item.ParticipantUserId" asp-route-view="balance" class="dropdown-item">
                                                    <i class="bi bi-wallet2"></i>مالی شرکت‌کننده
                                                </a>
                                            }
                                            @if (Model.IsAdmin && Model.AllowedActions.Contains("viewOrder") && item.EventId.HasValue)
                                            {
                                                <a asp-page="/Finance/TicketTransactions" asp-route-EventId="@item.EventId" class="dropdown-item">
                                                    <i class="bi bi-receipt-cutoff"></i>سفارش/تراکنش رویداد
                                                </a>
                                            }
                                            @if (Model.CanRefund && !item.IsRefunded && !item.IsRemoved)
                                            {
                                                <button type="button" class="dropdown-item text-danger" data-bs-toggle="collapse" data-bs-target="#refund-@item.TicketId">
                                                    <i class="bi bi-arrow-counterclockwise"></i>بازگشت اضطراری
                                                </button>
                                            }
                                        </div>
                                    </div>
                                    @if (Model.CanRefund && !item.IsRefunded && !item.IsRemoved)
                                    {
                                        <div class="collapse mt-2" id="refund-@item.TicketId">
                                            <form method="post" asp-page-handler="Refund" class="refund-form">
                                                <input type="hidden" asp-for="Input.TicketId" value="@item.TicketId" />
                                                <textarea asp-for="Input.Reason" class="form-control form-control-sm" rows="3" placeholder="دلیل حذف و بازگشت وجه"></textarea>
                                                <span asp-validation-for="Input.Reason" class="text-danger small"></span>
                                                <button type="submit" class="btn btn-sm btn-danger w-100"><i class="bi bi-check2-circle"></i>ثبت بازگشت وجه</button>
                                            </form>
                                        </div>
                                    }
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
        else
        {
            <div class="buyer-card-grid participant-card-grid">
                @foreach (var item in Model.Result.Items)
                {
                    <article class="buyer-check-card participant-card">
                        <img src="@(item.ProfileImageUrl ?? "/images/logo.png")" alt="@item.ParticipantName" class="buyer-card-photo" loading="lazy" />
                        <div class="buyer-card-body">
                            <div class="d-flex justify-content-between gap-2">
                                <div>
                                    <h3>@item.ParticipantName</h3>
                                    <p>
                                        @if (Model.IsEventScoped)
                                        {
                                            @PersianDateFormatter.FormatDate(item.PurchasedAtUtc, Model.IsRtl)
                                        }
                                        else
                                        {
                                            @item.EventTitle
                                        }
                                    </p>
                                </div>
                                <span class="status-badge @Model.GetTicketStatusClass(item)">@item.TicketStatus</span>
                            </div>
                            <div class="buyer-card-meta">
                                <span><i class="bi bi-person-vcard"></i>@item.ProfileStatusTitle</span>
                                <span><i class="bi bi-gender-ambiguous"></i>@item.GenderTitle</span>
                                <span><i class="bi bi-bag-check"></i>@item.BuyerName</span>
                                <span><i class="bi bi-geo-alt"></i>@item.CityTitle</span>
                                <span><i class="bi bi-ticket-perforated"></i>@DisplayFormatter.TicketMoney(item.TicketPrice, item.TicketCurrencyCode, Model.IsRtl)</span>
                                @if (Model.CanSeeContactInfo && !string.IsNullOrWhiteSpace(item.ParticipantMobile))
                                {
                                    <span dir="ltr"><i class="bi bi-phone"></i>@item.ParticipantMobile</span>
                                }
                            </div>
                            <div class="buyer-card-actions">
                                @if (Model.AllowedActions.Contains("viewDetails") && item.HasProfile)
                                {
                                    <a asp-page="/UserProfiles/Details" asp-route-userId="@item.ParticipantUserId" class="btn btn-sm btn-outline-primary">
                                        <i class="bi bi-person-bounding-box"></i>جزئیات
                                    </a>
                                }
                                @if (Model.IsAdmin && Model.AllowedActions.Contains("editProfile") && item.HasProfile)
                                {
                                    <a asp-page="/UserProfiles/AdminEdit" asp-route-userId="@item.ParticipantUserId" class="btn btn-sm btn-outline-secondary">
                                        <i class="bi bi-person-gear"></i>ویرایش
                                    </a>
                                }
                                @if (Model.IsAdmin && Model.AllowedActions.Contains("viewOrder") && item.EventId.HasValue)
                                {
                                    <a asp-page="/Finance/TicketTransactions" asp-route-EventId="@item.EventId" class="btn btn-sm btn-outline-secondary">
                                        <i class="bi bi-receipt-cutoff"></i>سفارش
                                    </a>
                                }
                            </div>
                        </div>
                    </article>
                }
            </div>
        }

        @if (Model.Result.Items.Count == 0)
        {
            <div class="empty-state">برای فیلتر فعلی شرکت‌کننده‌ای پیدا نشد.</div>
        }
    </section>
}

<nav class="d-flex justify-content-between align-items-center flex-wrap gap-3 mt-4">
    <a class="btn btn-outline-secondary @(Model.HasPreviousPage ? string.Empty : "disabled")"
       asp-page="/Participants/Index"
       asp-route-EventId="@Model.EventId"
       asp-route-Search="@Model.Search"
       asp-route-ProfileStatus="@Model.ProfileStatus"
       asp-route-TicketStatus="@Model.TicketStatus"
       asp-route-Gender="@Model.Gender"
       asp-route-CityId="@Model.CityId"
       asp-route-ZodiacSignId="@Model.ZodiacSignId"
       asp-route-IsActive="@Model.IsActive"
       asp-route-FilterEventId="@Model.FilterEventId"
       asp-route-BuyerUserId="@Model.BuyerUserId"
       asp-route-TicketOrderId="@Model.TicketOrderId"
       asp-route-FromDate="@Model.FromDate?.ToString("yyyy-MM-dd")"
       asp-route-ToDate="@Model.ToDate?.ToString("yyyy-MM-dd")"
       asp-route-Sort="@Model.Sort"
       asp-route-View="@Model.View"
       asp-route-PageSize="@Model.PageSize"
       asp-route-PageNumber="@(Model.PageNumber - 1)">قبلی</a>
    <span class="text-muted small">صفحه @DisplayFormatter.Count(Model.PageNumber) از @DisplayFormatter.Count(Model.TotalPages)</span>
    <a class="btn btn-outline-secondary @(Model.HasNextPage ? string.Empty : "disabled")"
       asp-page="/Participants/Index"
       asp-route-EventId="@Model.EventId"
       asp-route-Search="@Model.Search"
       asp-route-ProfileStatus="@Model.ProfileStatus"
       asp-route-TicketStatus="@Model.TicketStatus"
       asp-route-Gender="@Model.Gender"
       asp-route-CityId="@Model.CityId"
       asp-route-ZodiacSignId="@Model.ZodiacSignId"
       asp-route-IsActive="@Model.IsActive"
       asp-route-FilterEventId="@Model.FilterEventId"
       asp-route-BuyerUserId="@Model.BuyerUserId"
       asp-route-TicketOrderId="@Model.TicketOrderId"
       asp-route-FromDate="@Model.FromDate?.ToString("yyyy-MM-dd")"
       asp-route-ToDate="@Model.ToDate?.ToString("yyyy-MM-dd")"
       asp-route-Sort="@Model.Sort"
       asp-route-View="@Model.View"
       asp-route-PageSize="@Model.PageSize"
       asp-route-PageNumber="@(Model.PageNumber + 1)">بعدی</a>
</nav>

<partial name="_ValidationScriptsPartial" />
