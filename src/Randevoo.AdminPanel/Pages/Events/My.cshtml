@page
@model Randevoo.AdminPanel.Pages.Events.MyModel
@{
    ViewData["Title"] = "رویدادهای من";
}

<section class="page-header">
    <div>
        <h1>رویدادهای من</h1>
        <p>هر تغییر پس از ثبت دوباره برای تایید مدیر ارسال می شود و بعد از تایید نمایش داده خواهد شد.</p>
    </div>
    <a asp-page="/Events/Edit" class="btn btn-primary">ساخت رویداد</a>
</section>

@if (!string.IsNullOrWhiteSpace(Model.StatusMessage))
{
    <div class="alert alert-success border-0 mb-4">@Model.StatusMessage</div>
}

<div class="glass-panel p-4">
    <div class="table-responsive">
        <table class="table align-middle">
            <thead>
                <tr>
                    <th>رویداد</th>
                    <th>زمان شروع</th>
                    <th>وضعیت</th>
                    <th>نظر مدیر</th>
                    <th>اقدام ها</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.Events)
                {
                    <tr>
                        <td>
                            <div class="fw-semibold">@item.DisplayTitle</div>
                            <div class="text-muted small">@item.ActiveDraft.City</div>
                            @if (item.ActiveDraft.Tags.Count > 0)
                            {
                                <div class="event-chip-list mt-2">
                                    @foreach (var tag in item.ActiveDraft.Tags.Take(3))
                                    {
                                        <span class="event-chip">@tag</span>
                                    }
                                </div>
                            }
                        </td>
                        <td>@PersianDateFormatter.Format(item.ActiveDraft.StartAtUtc, Model.IsRtl)</td>
                        <td><span class="status-badge @Model.GetStatusClass(item.Status)">@DisplayFormatter.Status(item.Status)</span></td>
                        <td class="text-muted">@item.AdminReviewNote</td>
                        <td class="text-end table-action-cell">
                            <div class="dropdown">
                                <button class="btn btn-sm btn-outline-secondary dropdown-toggle table-action-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                                    اقدام ها
                                </button>
                                <div class="dropdown-menu dropdown-menu-end action-menu shadow-sm border-0">
                                    <a asp-page="/Events/Details" asp-route-id="@item.Id" class="dropdown-item">پروفایل رویداد</a>
                                    <a asp-page="/Events/Edit" asp-route-id="@item.Id" class="dropdown-item">ویرایش رویداد</a>
                                    <a asp-page="/Events/Buyers" asp-route-eventId="@item.Id" class="dropdown-item">خریداران بلیت</a>
                                    <a asp-page="/Events/Sms" asp-route-eventId="@item.Id" class="dropdown-item">پیام به شرکت کنندگان</a>
                                    <a asp-page="/Public/Event" asp-route-id="@item.Id" class="dropdown-item">نمایش عمومی</a>
                                    <div class="dropdown-divider"></div>
                                    <form method="post" asp-page-handler="OpenSale">
                                        <input type="hidden" name="id" value="@item.Id" />
                                        <button type="submit" class="dropdown-item">درخواست فروش</button>
                                    </form>
                                    <form method="post" asp-page-handler="CloseSale">
                                        <input type="hidden" name="id" value="@item.Id" />
                                        <button type="submit" class="dropdown-item">درخواست بستن فروش</button>
                                    </form>
                                    <form method="post" asp-page-handler="Cancel">
                                        <input type="hidden" name="id" value="@item.Id" />
                                        <button type="submit" class="dropdown-item text-danger">درخواست لغو</button>
                                    </form>
                                </div>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
